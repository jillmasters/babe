import React from 'react';
import { MainView } from '../theme';

const Settings = () => {
  return (
    <MainView>
      <h4>Reconfigure settings</h4>
    </MainView>
  );
};

export default Settings;
